// Postgres/Supabase connection — stub for future migration
// Install: npm install @neondatabase/serverless or pg
// import { drizzle } from 'drizzle-orm/neon-http'
// import { neon } from '@neondatabase/serverless'
// import * as schema from './schema.pg'

// const sql = neon(process.env.DATABASE_URL!)
// export const postgresDb = drizzle(sql, { schema })

export const postgresDb = null as any // placeholder
